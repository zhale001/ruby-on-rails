<div>
	<blockquote>
	
	<p><%= comment.body %></p>
	<h6> comment by <%= comment.author_name %> posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later </h6>

	</blockquote>
</div>
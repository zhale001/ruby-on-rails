<h1> <strong>Deez</strong> Bloggings</h1>

<ul id="articles">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article), class: 'article_title' %>

    </li>
  <% end %>
<br>
  <%= link_to "Make A New Blogging, Dude", new_article_path, class: 'button big' %>
</ul>